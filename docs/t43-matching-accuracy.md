# T43 매칭 정확도 중간 측정 (WEB-43)

**측정 시각**: 2026-09-08 · **기준**: T18 정답 픽스처 35건
**재생성**: `npm run measure:matching`

## 요약

| 지표 | 값 |
| --- | --- |
| 자동 확정률 (엔진이 ①② 로 처리) | **54.3%** |
| 이 픽스처의 자동 확정 상한 (정답이 ①②) | 54.3% |
| 정답 경로 재현율 | **100.0%** |
| 정답 작품 발견율 (확정 + 후보 포함) | **100.0%** |
| 오확정(자동 확정했는데 틀림) | **0건** |

## 경로별

| 정답 경로 | 케이스 | 경로 일치 | 정답 발견 |
| --- | --- | --- | --- |
| ① 외부 ID | 6 | 6/6 | 6/6 |
| ② 제목+연도 | 13 | 13/13 | 13/13 |
| ③ 후보 제시 | 10 | 10/10 | 10/10 |
| ④ 스킵 | 6 | 6/6 | 6/6 |

## 어긋난 케이스

없음.
## 해석 — 90% 목표와의 갭 (T43 완료 조건)

**픽스처 자동 확정률 54.3% 를 §5.3 의 90% 와 직접 비교하면 안 된다.** 이 표는 실사용 분포가 아니라 어려운 케이스를 일부러 모은 것이고, 정답 자체가 ①② 인 행이 54.3% 뿐이다. 즉 상한을 이미 채우고 있다.

실사용 기준 추정은 이렇다.

- **Steam(T19)**: WEB-3 실검증에서 보유 236건 중 225건이 appid 로 직접 매칭됐다(95.3%, 플레이 시간 가중 92.1%). appid 는 경로 ① 이므로 **Steam 만 놓고 보면 90% 목표는 이미 넘는다.** 남는 11건은 매칭 실패가 아니라 게임이 아닌 항목(Wallpaper Engine)·베타/테스트 빌드다.
- **CSV(T20)**: Letterboxd 내보내기에는 외부 ID 가 없다. 영화 전량이 ②③④ 로 내려오므로 **90% 달성의 실제 관문은 여기다.** T20 에서 실제 내보내기 파일로 다시 측정해야 한다.

**보완 방향**

1. 병목은 매칭 규칙이 아니라 **카탈로그 커버리지**다. 스킵 케이스는 대부분 '못 알아본 것'이 아니라 '카탈로그에 없는 것'이다. T19·T20 에서 스킵 직전에 온디맨드 적재(T11·T12)를 한 번 태우면 스킵의 상당수가 ①② 로 내려온다.
2. **Steam 은 출시 연도를 주지 않는다.** 연도가 없으면 ② 는 제목이 유일할 때만, ③ 은 유사도 85% 이상일 때만 걸리도록 좁혀 뒀다. IGDB 적재 시 연도를 확보하면 이 제약이 풀린다.
3. 나머지는 확인 UX(T44)가 받는다 — 후보 3개 제시는 §5.3 의 설계 전제이지 실패 처리가 아니다.

> ⚠ 재현율 100% 를 성능 근거로 쓰지 않는다. 픽스처와 엔진을 같은 시점에 함께 설계했으므로 이 숫자는 '설계대로 동작한다'는 뜻이지 '실데이터에서 잘 맞는다'는 뜻이 아니다. 실측은 T19·T20 에서 본인 라이브러리·실제 CSV 로 한다.
